import { makeStyles } from '@material-ui/core/styles';

const patientPageStyle = makeStyles(() => ({
  root: {
    padding: 50,
    marginTop: 50
  },

  shiftRight: {
    padding: 50,
    marginTop: 50,
    marginLeft: 240
  }
}));

export default patientPageStyle;
